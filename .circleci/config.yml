# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

jobs:
  create_infrastructure: #job name
    docker: # it uses the docker executor
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Ensure background infrastructure exist
          #echo environment variable
          command: |
            aws cloudformation deploy --template-file template.yml --stack-name my-stack
  create_docker:
    docker:
      - image: python:3.7-alphine3.11
    steps:
      - add_ssh_keys:
          fingerprints:
            - "54:77:1c:30:84:c7:1b:a8:dd:5e:27:85:b6:87:1e:31"

workflows:
  # workflow
  my_workflow:
    jobs:
      - create_infrastructure
      # - create_docker:
      #     requires:
      #       - create_infrastructure
